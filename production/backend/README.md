# Langou Backend

Private FastAPI service for the Android and Windows clients.

Production runs behind `api.langou.tech`; screenshots are never accepted by
the API. Local development defaults to SQLite and in-memory infrastructure,
while production requires PostgreSQL and Redis.

